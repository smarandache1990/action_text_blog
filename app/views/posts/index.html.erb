<h1>Posts</h1>

<h2>This blog currently has <%= pluralize(Post.public_count, "post") %> and counting </h2>

<% @posts.each do |post| %>
    <h1><%= link_to post.title, post_url(post) %></h1>
    <p><%= post.body %></p>
    <br>
<% end %>

<%= link_to "New Post", new_post_url %>
